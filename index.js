
// index.js
const name = "Susan"; // Changed from "Joe"
const height = 35;    // Changed from 74 (must be < 40)
const message = `${name} is ${height} inches tall`;

module.exports = { name, height, message };